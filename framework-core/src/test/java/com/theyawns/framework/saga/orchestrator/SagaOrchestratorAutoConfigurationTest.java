package com.theyawns.framework.saga.orchestrator;

import com.theyawns.framework.saga.SagaState;
import com.theyawns.framework.saga.SagaStateStore;
import com.theyawns.framework.saga.SagaStatus;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Nested;
import org.junit.jupiter.api.Test;
import org.springframework.boot.autoconfigure.AutoConfigurations;
import org.springframework.boot.test.context.runner.ApplicationContextRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.time.Duration;
import java.util.List;
import java.util.Optional;

import static org.assertj.core.api.Assertions.assertThat;
import static org.mockito.Mockito.mock;

/**
 * Tests for {@link SagaOrchestratorAutoConfiguration}.
 *
 * @author Generated by Claude Code
 * @since 3.0
 */
@DisplayName("SagaOrchestratorAutoConfiguration - Auto-configuration behavior")
class SagaOrchestratorAutoConfigurationTest {

    private final ApplicationContextRunner contextRunner = new ApplicationContextRunner()
            .withConfiguration(AutoConfigurations.of(SagaOrchestratorAutoConfiguration.class));

    @Nested
    @DisplayName("When SagaStateStore is present")
    class WhenSagaStateStorePresent {

        @Test
        @DisplayName("should create HazelcastSagaOrchestrator bean")
        void shouldCreateOrchestratorBean() {
            contextRunner
                    .withUserConfiguration(SagaStateStoreConfig.class)
                    .run(context -> {
                        assertThat(context).hasSingleBean(HazelcastSagaOrchestrator.class);
                    });
        }

        @Test
        @DisplayName("should create LoggingSagaOrchestratorListener bean")
        void shouldCreateLoggingListenerBean() {
            contextRunner
                    .withUserConfiguration(SagaStateStoreConfig.class)
                    .run(context -> {
                        assertThat(context).hasSingleBean(LoggingSagaOrchestratorListener.class);
                    });
        }

        @Test
        @DisplayName("should create ScheduledExecutorService bean")
        void shouldCreateSchedulerBean() {
            contextRunner
                    .withUserConfiguration(SagaStateStoreConfig.class)
                    .run(context -> {
                        assertThat(context).hasBean("sagaOrchestratorScheduler");
                    });
        }
    }

    @Nested
    @DisplayName("When SagaStateStore is absent")
    class WhenSagaStateStoreAbsent {

        @Test
        @DisplayName("should not create orchestrator bean")
        void shouldNotCreateOrchestratorBean() {
            contextRunner
                    .run(context -> {
                        assertThat(context).doesNotHaveBean(HazelcastSagaOrchestrator.class);
                    });
        }

        @Test
        @DisplayName("should not create logging listener bean")
        void shouldNotCreateLoggingListenerBean() {
            contextRunner
                    .run(context -> {
                        assertThat(context).doesNotHaveBean(LoggingSagaOrchestratorListener.class);
                    });
        }
    }

    @Configuration
    static class SagaStateStoreConfig {

        @Bean
        SagaStateStore sagaStateStore() {
            return mock(SagaStateStore.class);
        }
    }
}
