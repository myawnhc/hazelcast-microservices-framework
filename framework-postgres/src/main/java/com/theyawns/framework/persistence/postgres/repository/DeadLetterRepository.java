package com.theyawns.framework.persistence.postgres.repository;

import com.theyawns.framework.persistence.postgres.entity.DeadLetterEntity;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.Optional;

/**
 * Spring Data JPA repository for the {@code domain_dead_letters} table.
 *
 * @author Generated by Claude Code
 * @since 3.0
 */
@Repository
public interface DeadLetterRepository extends JpaRepository<DeadLetterEntity, String> {

    /**
     * Finds a dead letter entry by its DLQ entry ID.
     *
     * @param dlqEntryId the DLQ entry ID
     * @return the entity if found
     */
    Optional<DeadLetterEntity> findByDlqEntryId(String dlqEntryId);

    /**
     * Finds all DLQ entry IDs where the status is PENDING.
     * Used for recovery on startup to restore entries that need attention.
     *
     * @return DLQ entry IDs of PENDING entries
     */
    @Query("SELECT d.dlqEntryId FROM DeadLetterEntity d WHERE d.status = 'PENDING'")
    List<String> findAllPendingDlqEntryIds();
}
