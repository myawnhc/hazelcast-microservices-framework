# A/B Test Configuration Profile: Profiling Overhead
# Baseline configuration with async-profiler attached to measure profiling overhead.
# Compare against community-baseline to see how much profiling costs.

NAME=profiling
DESCRIPTION=Community baseline with async-profiler attached
COMPOSE_FILES=docker-compose.yml
ENV_OVERRIDES=
SPRING_PROFILES=
K6_SCENARIO=constant
K6_TPS=50
K6_DURATION=3m
PRE_HOOK=scripts/perf/profile-service.sh --service order-service --duration 180 --background
POST_HOOK=
MEMORY_LIMIT=
NOTES=Measures profiling overhead by running async-profiler during the test
