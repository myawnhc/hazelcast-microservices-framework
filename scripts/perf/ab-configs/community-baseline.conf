# A/B Test Configuration Profile: Community Baseline
# Default Community Edition setup — no Enterprise features, no overrides.
# Use this as the control/baseline for all comparisons.

NAME=community-baseline
DESCRIPTION=Community Edition baseline with default settings
COMPOSE_FILES=docker-compose.yml
ENV_OVERRIDES=
SPRING_PROFILES=
K6_SCENARIO=constant
K6_TPS=50
K6_DURATION=3m
PRE_HOOK=
POST_HOOK=
MEMORY_LIMIT=
NOTES=Default Community Edition, 3-node Hazelcast cluster, 512M per service
