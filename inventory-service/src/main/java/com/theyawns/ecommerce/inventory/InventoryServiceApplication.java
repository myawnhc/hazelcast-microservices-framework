package com.theyawns.ecommerce.inventory;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot application entry point for the Inventory Service.
 *
 * <p>The Inventory Service manages product inventory including:
 * <ul>
 *   <li>Product creation and management</li>
 *   <li>Stock reservation for orders</li>
 *   <li>Stock release for cancelled orders</li>
 *   <li>Inventory availability queries</li>
 * </ul>
 *
 * <p>This service uses the event sourcing framework to:
 * <ul>
 *   <li>Publish domain events for all inventory operations</li>
 *   <li>Maintain a materialized view for fast product lookups</li>
 *   <li>Support event replay for view reconstruction</li>
 * </ul>
 *
 * @author Generated by Claude Code
 * @since 1.0
 */
@SpringBootApplication(scanBasePackages = {
        "com.theyawns.ecommerce.inventory",
        "com.theyawns.framework"
})
public class InventoryServiceApplication {

    /**
     * Main entry point for the Inventory Service.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(InventoryServiceApplication.class, args);
    }
}
