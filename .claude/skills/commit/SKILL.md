# Commit Changes

1. Run `mvn clean test` from project root to verify all tests pass
2. Run `git diff --stat` to review all changes
3. Stage all changes with `git add -A`
4. Create commit message in format: "Phase X Day Y: Brief description\n\n- Bullet points of key changes"
5. Commit (do NOT push unless explicitly asked)
