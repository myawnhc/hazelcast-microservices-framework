apiVersion: v1
kind: Service
metadata:
  name: {{ include "management-center.fullname" . }}
  labels:
    {{- include "management-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "management-center.selectorLabels" . | nindent 4 }}
