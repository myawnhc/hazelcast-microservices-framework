Order Service has been deployed!

  Service URL (cluster-internal): http://{{ include "order-service.fullname" . }}:{{ .Values.service.port }}
  API base path: /api/orders

To check pod status:
  kubectl get pods -l app.kubernetes.io/name={{ include "order-service.name" . }}

To view logs:
  kubectl logs -l app.kubernetes.io/name={{ include "order-service.name" . }} -f

To port-forward for local access:
  kubectl port-forward svc/{{ include "order-service.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

Then access: http://localhost:{{ .Values.service.port }}/api/orders
