package com.theyawns.ecommerce.order;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot application entry point for the Order Service.
 *
 * <p>The Order Service manages customer orders including:
 * <ul>
 *   <li>Order creation with line items</li>
 *   <li>Order confirmation (after stock reservation)</li>
 *   <li>Order cancellation with compensating actions</li>
 *   <li>Order status tracking</li>
 * </ul>
 *
 * <p>This service uses the event sourcing framework to:
 * <ul>
 *   <li>Publish domain events for all order operations</li>
 *   <li>Maintain a materialized view for fast order lookups</li>
 *   <li>Support event replay for view reconstruction</li>
 * </ul>
 *
 * @author Generated by Claude Code
 * @since 1.0
 */
@SpringBootApplication(scanBasePackages = {
        "com.theyawns.ecommerce.order",
        "com.theyawns.framework"
})
public class OrderServiceApplication {

    /**
     * Main entry point for the Order Service.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(OrderServiceApplication.class, args);
    }
}
