package com.theyawns.ecommerce.mcp;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * MCP (Model Context Protocol) server for AI assistant integration.
 *
 * <p>Provides tools that allow AI assistants to interact with the eCommerce
 * microservices via REST proxy. The server uses stdio transport for communication
 * and does not start a web server.
 *
 * <p>Available tools:
 * <ul>
 *   <li>{@code query_view} - Query materialized views (customer, product, order, payment)</li>
 *   <li>{@code submit_event} - Submit domain events (create, cancel, reserve, etc.)</li>
 *   <li>{@code get_event_history} - Retrieve event history for domain entities</li>
 *   <li>{@code inspect_saga} - Inspect saga state by ID (status, steps, timing)</li>
 *   <li>{@code list_sagas} - List sagas with optional status filter</li>
 *   <li>{@code get_metrics} - Get system metrics (saga counts, event throughput)</li>
 *   <li>{@code run_demo} - Run demo scenarios (happy_path, payment_failure, etc.)</li>
 * </ul>
 *
 * @author Generated by Claude Code
 * @since 2.0
 */
@SpringBootApplication
public class McpServerApplication {

    public static void main(String[] args) {
        SpringApplication.run(McpServerApplication.class, args);
    }
}
